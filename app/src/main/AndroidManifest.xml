<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="me.brunofelix.googlecertification">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/ThemeGoogleCertApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.taskdetails.TaskDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.taskadd.TaskAddActivity"
            android:exported="false" />
        <activity
            android:name=".ui.tasklist.TaskListActivity"
            android:exported="true"
            android:theme="@style/ThemeGoogleCertApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>